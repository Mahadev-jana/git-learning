git commmand

 git add .
 git commit -m "add testing text"

 git push origin main

 get fetch

git branch -D mahadev

stash → fetch → review → merge → stash pop → resolve → commit

git stash
 git status

 nothing to commit, working tree clean

 git fetch origin

 git log main..origin/feature/login

 git diff main..origin/feature/login    

 git merge origin/feature/login

 git add .
git commit -m "Resolved merge conflict"

git stash pop

git add .
git commit -m "Resolved stash conflict" 